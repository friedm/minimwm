ORGANIZATION!:
tile, desktop, screen, and window .c

BUGS:
can adjust window width to negative, cause crash
window width can travel
spotify can still crash if it is force quit

FEATURES:
actions to cycle current screen
action to move current window to next screen's desktop
when another screen has a desktop, and the current screen wants to swap to it,
   also swap the other screen (exchange desktops) -- no two screens should ever point to the same desktop

key configuration from file

do something about fullscreen?
